---
description: Report a bug or small change to project-manager, which tracks it and gets developer to build it
---

Spawn the `project-manager` agent with the following bug report or change request:

$ARGUMENTS

Give it the full context above verbatim. It should track this as a GitHub issue (create one if none already covers it), then spawn `developer` to build it against that issue. Relay its response once it's done, including the issue number and what `developer` reported back.
