---
description: Bring a new feature or bigger idea to the architect agent for intake, UX review, and ticketing
---

Spawn the `architect` agent with the following feature/idea to think through:

$ARGUMENTS

Give it the full context above verbatim. It should think the idea through at the architecture level, spawn `ux` itself if the idea touches `tcg_inventory`'s UI, and file a GitHub issue for it once settled — then either hand that ticket to `project-manager` or report it back here, whichever it judges fits. Relay its response once it's done.
